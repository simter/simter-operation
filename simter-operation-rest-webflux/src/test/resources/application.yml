spring.main.banner-mode: 'off'
logging:
  file.name: target/test.log
  level: # TRACE < DEBUG < INFO < WARN < ERROR < FATAL < OFF
    root: ERROR
    tech.simter.operation: ERROR
simter-operation.rest-context-path: /